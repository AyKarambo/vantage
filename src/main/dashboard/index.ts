/** Public surface of the dashboard window; import from './dashboard', not from its siblings. */
// The renderer-facing data contract.
export type { DataProvider } from './provider';
// The window itself.
export { DashboardWindow } from './dashboardWindow';
